git init -- Initialize the Empty Repo
git add . -- add the files
git commit -m "" -- Giving Commit messages
git branch -M main/master -- defining a branch
git remote add origin <Location of the Repo> -- giving Remote Location
git push -u origin main/master -- pushing the code

git add . -- add the files
git commit -m "" -- Giving Commit messages
git push origin main/master -- pushing to the branch


git status -- status of the application
git log -- for checking logs
git remote -v --  to check location of git

git clone <repo Link> -- clone the other public Repository
git pull origin main/master -- pull the latest code from the code 


git branch <branch name> -- creating new branches
git branch -- checking all branches
git checkout <branch name> -- one branch to other branch